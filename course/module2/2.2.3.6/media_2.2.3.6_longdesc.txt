Estas figuras se basan en el diagrama de laboratorio estándar.  R1 con una LAN se conecta a R2 con una LAN que se conecta a R3 con una LAN. La figura uno muestra que el resultado del comando “show ipv6 route static” en el router 1. La figura dos muestra el resultado de “show ipv6 route” para una red específica. La figura tres muestra el resultado del comando “show running guión config barra vertical section ipv6 route”.  