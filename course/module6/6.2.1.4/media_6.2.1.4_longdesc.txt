La figura uno muestra una tabla con los comandos de Cisco IOS necesarios para eliminar un puerto de switch de una VLAN. La figura dos muestra la sintaxis para eliminar el puerto F0/18 de una VLAN y el resultado del comando “show vlan brief” para verificar que el puerto F0/18 vuelva a la VLAN uno. La figura tres muestra el resultado del comando "show interfaces F0/18". La figura cuatro muestra la sintaxis para configurar F0/11 como puerto de acceso y miembro de la VLAN 20.  También se muestra el resultado del comando “show vlan brief” para verificar que el puerto esté asignado a la VLAN correcta. La figura cinco es una actividad interactiva que permite a los alumnos practicar trabajar con un switch y asignarlo y desasignarlo a una VLAN.