La figura uno muestra una tabla con los comandos para restablecer la VLAN nativa y restablecer las VLAN permitidas en un enlace troncal. La figura dos muestra la sintaxis para restablecer la VLAN nativa y restablecer las VLAN permitidas en un enlace troncal. También se muestra el resultado del comando “show interfaces F0/1 switchport” para verificar que el puerto se reconfiguró correctamente. La figura tres muestra la sintaxis para configurar F0/1 como un puerto de acceso. También se muestra el comando “show interfaces f0/1 switchport” para verificar que el puerto se reconfiguró correctamente.