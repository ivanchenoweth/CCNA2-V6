La figura muestra una LAN con una PC conectada al switch 3 y un servidor Web conectado al switch 1.  Los enlaces entre los switches son enlaces troncales con las VLAN adecuadas permitidas.  La PC tiene una llamada que indica que no puede conectarse al servidor Web. La figura dos muestra el resultado del comando “show interfaces trunk” en el switch 1 y el comando “show interface f0/3 switchport” en el switch 1. El modo de F0/3 se resalta y está configurado como dinámico automático.  También se muestra el resultado del comando “show interfaces trunk” en el switch 3.  No hay enlaces troncales en el switch 3.  También se muestra el resultado del comando “show interfaces f0/3” en el switch 3.  El modo de puerto se resalta y se configura como "dynamic auto". La figura 3 muestra la sintaxis para configurar el puerto F0/3 en los switches 1 y 3 como puerto de enlace troncal y el resultado de un ping exitoso desde la PC al servidor Web.