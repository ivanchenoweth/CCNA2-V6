La figura 1 muestra dos routers conectados al mismo switch.  El router uno está configurado como un servidor DHCPv6 con estado y el router tres es un cliente DHCPv6.  La figura muestra la CLI para el router uno y muestra el resultado del comando show ipv6 dhcp pool.  En esta figura se resaltan el prefijo de asignación de dirección y la línea que indica que hay un cliente activo. La figura dos muestra el resultado del comando show ipv6 dhcp binding.  En esta figura se resaltan la dirección local de enlace del router tres y la dirección DHCPv6 asignada a él por el router uno.  La figura tres muestra que el router tres ejecuta el comando show ipv6 interface g 0 barra 1. En esta figura se resaltan la dirección IPv6 y la dirección del router predeterminado asignada por servidor DHCPv6. La figura 4 es una actividad interactiva que permite que el estudiante configure y verifique DHCPv6 en un router.