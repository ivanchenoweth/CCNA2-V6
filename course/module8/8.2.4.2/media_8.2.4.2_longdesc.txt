La figura uno enumera los comandos utilizados para configurar los servicios DHCPv6 con estado en un router.  El ejemplo comienza en el modo de configuración global y los comandos son los siguientes.  El primer comando es ipv6 unicast guion routing.  El siguiente comando es dhcp pool ipv6  guión stateful.  El siguiente comando es address prefix 2 0 0 1 dos puntos d b 8 dos puntos c a f e dos puntos 1 dos puntos dos puntos barra 64 lifetime infinite.  El siguiente comando es dns guion server 2 0 0 1 dos puntos d b 8 dos puntos c a f e dos puntos a a a a dos puntos dos puntos 5.  El siguiente comando es domain guion ejemplo punto com.  El conjunto DHCPv6 se ha configurado y el siguiente conjunto de comandos vincula el conjunto a la interfaz.  La CLI del ejemplo pasa a la interfaz local primero con el comando exit. Al comando exit lo sigue el comando interface g cero barra uno.  El siguiente comando es ipv6 address 2 0 0 1 dos puntos d b 8 dos puntos c a f e dos puntos 1 dos puntos dos puntos 1 barra 64.  El siguiente comando es dhcp pool ipv6.  guion stateful.  El último comando es ipv6 nd managed guion config guion flag.  Se destaca el comando address prefix y el comando administrado ipv6 nd para indicar que estos comandos son específicos de la configuración con estado.  La figura también enumera los comandos para configurar los servicios DHCPv6 sin estado.  El ejemplo comienza en el modo de configuración global y los comandos son los siguientes.  El primer comando es ipv6 unicast guion routing.  El siguiente comando es dhcp pool ipv6 guion stateless.  El siguiente comando es dns guion server 2 0 0 1 dos puntos d b 8 dos puntos c a f e dos puntos a a a a dos puntos dos puntos 5.  El siguiente comando es domain guion ejemplo punto com.  El conjunto DHCPv6 se ha configurado y el siguiente conjunto de comandos vincula el conjunto a la interfaz.  La CLI del ejemplo pasa a la interfaz local primero con el comando exit. Al comando exit lo sigue el comando interface g cero barra uno.  El siguiente comando es ipv6 address 2 0 0 1 dos puntos d b 8 dos puntos c a f e dos puntos 1 dos puntos dos puntos 1 barra 64.  El siguiente comando es dhcp pool ipv6 guion stateless.  El último comando es ipv6 nd other guion config guion flag.  El comando ipv6 nd other guión config guión flag se resalta en la figura para indicar que es específico de la configuración DHCPv6 sin estado. La figura dos muestra el resultado de tres configuraciones diferentes cuando se ejecuta el comando show ipv6 interface en una interfaz específica del router.  El primer resultado es para SLAAC, y la figura resalta la línea que indica que “hosts use stateless autoconfig for address” (los hosts utilizan asegura la configuración automática sin estado para la dirección).  El siguiente resultado es para DHCPv6 sin estado., y la figura resalta la línea que indica que “hosts use DHCP to obtain other configuration” (los hosts utilizan DHCP para obtener otra configuración).  El tercer resultado es para DHCPv6 con estado, y la figura resalta la línea que indica que “hosts use DHCP to obtain routable addresses” (los hosts utilizan DHCP para obtener direcciones enrutables).