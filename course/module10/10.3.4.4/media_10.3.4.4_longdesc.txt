En la figura número uno se muestra la salida del comando "show license u. d. i." en el router uno. En la figura número dos se muestra un router con una etiqueta extraíble en la que se indica el UDI y el PID correspondientes al router. En la figura número tres se muestra una actividad interactiva que permite que el alumno practique cómo mostrar el UDI correspondiente a un router en la línea de comandos.