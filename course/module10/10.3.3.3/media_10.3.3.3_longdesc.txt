En la figura número uno se muestra un router y un servidor para demostrar que el IOS se puede copiar de un servidor TFTP al router. En la figura número dos se muestra el resultado exitoso de una operación de ping del router al servidor. En la figura número tres se muestra la salida del comando "show flash colon zero" en el router. En la figura número cuatro se muestra la sintaxis para copiar el IOS del servidor TFTP a la memoria flash del router.