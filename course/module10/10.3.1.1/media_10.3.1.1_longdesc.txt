En la Figura 1 se muestra la salida del comando show file systems en un router. En la Figura 1 se muestra la salida del comando dir en un router.  The output displays the contents of flash. En la Figura 3 se muestra la salida del comando cd nvram:, seguida por el comando pwd.  El comando pwd "imprime el directorio de trabajo".  Luego, se emite el comando dir para incluir el contenido de NVRAM en una lista.