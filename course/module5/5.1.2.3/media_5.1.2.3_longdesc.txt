En la figura se muestra una tabla que enumera los comandos utilizados para configurar la función MDIX en una interfaz de puerto de switch.  La CLI comienza en el mode EXEC con privilegios y el primer comando es configure terminal.  Esto pasa al modo de configuración global.  El siguiente comando es interface fast Ethernet cero barra uno, y la línea del switch se transforma en config guion if entre paréntesis.  El siguiente comando es duplex full.  El siguiente comando es speed 100.  El nuevo comando es mdix auto, que establece la opción de mdix como automático. La tabla termina con el comando copy running guión config startup guión config. La figura dos muestra el resultado del comando show controllers Ethernet guión controller f.a. cero barra uno phy barra vertical include auto guión mdix. La figura tres es una actividad interactiva que permite que el estudiante configure una interfaz de puerto de switch con la opción mdix.