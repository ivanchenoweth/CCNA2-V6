La figura uno muestra el resultado del comando show ip protocols y se resalta la línea que indica que el resumen automático de la red está en vigencia. La figura dos muestra cómo desactivar el resumen de ruta en un router que usa RIP. La línea de comandos comienza en el modo de configuración global y el primer comando habilita RIP, que es router rip.  El siguiente comando es no auto guión summary.  Este comando se verifica mediante el comando show ip protocols barra vertical section Automatic, y el resultado indica que el resumen automático de red no está en vigencia. La figura tres es un elemento de recursos interactivos que permite que el estudiante configure el routing RIP y deshabilite el resumen automático en un router.