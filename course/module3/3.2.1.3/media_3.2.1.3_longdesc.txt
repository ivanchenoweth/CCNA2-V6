La figura dos muestra el resultado del comando show ip route.  Este router está ejecutando RIP versión 1 y el resultado muestra los valores enumerados en la lectura.  La figura dos muestra el resultado del comando show ip route barra vertical begin Gateway.  Esto enumera la tabla de routing que muestra dos rutas conectadas, dos direcciones IP de interfaz y tres rutas de red descubiertas mediante el protocolo RIP.  Estas tres redes están enumeradas con una R mayúscula en la tabla, lo que significa que se descubrieron a través de actualizaciones de RIP. La figura tres es un elemento de recursos interactivos que permite que el estudiante utilice el comando show ip protocols y el comando show ip route para ver la configuración del protocolo de routing actual y la tabla de routing para un router.