La figura uno muestra el resultado del comando show ip protocols y se resalta la sección que indica que el router envía la versión uno, pero puede recibir actualizaciones de la versión uno y la versión dos. La figura dos muestra los comandos necesarios para habilitar RIP versión 2 en un router.  La figura comienza en el modo de configuración global y habilita el RIP con el comando router rip.  El siguiente comando es version 2, que posibilita el RIP versión 2 y deshabilita el RIP versión 1.  Se muestra el resultado del comando show ip protocols, y ahora el router envía y recibe solo la versión 2 del RIP. La figura tres muestra el resultado del comando show ip route barra vertical begin Gateway. Esto muestra la tabla de routing, que no tiene rutas de RIP ya que en la figura anterior el comando version 2 habilitó solamente la versión 2 en el router y ahora se ignoran las actualizaciones de RIP versión 1. La figura cuatro es un elemento de recursos interactivos que permite que el estudiante configure el RIP versión 2 en un router.