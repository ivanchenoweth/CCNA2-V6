La figura uno muestra el resultado del comando show ip interfaces brief en un router.  En el resultado hay dos interfaces gigabit, dos interfaces seriales, la dirección IP y el estado operativo actual de cada una. La figura dos muestra el resultado del comando show ip route en un router.  Este router tiene tres interfaces configuradas, por lo que hay tres rutas conectadas identificadas en la primera columna con la letra C y tres rutas locales identificadas en la primera columna con la letra L. En la figura tres se muestra el resultado del comando show running-config interface gigabit Ethernet cero barra cero.  Este resultado muestra información más detallada sobre la interfaz especificada, incluida la máscara de subred. La figura cuatro es una actividad interactiva que permite que el estudiante ejecute el comando show interfaces gigabit ethernet cero barra cero y observe el resultado. El cuadro cinco es una actividad interactiva que permite que el estudiante ejecute Ethernet Gigabit del comando show i.p cero comando cero de barra diagonal y observe el resultado.