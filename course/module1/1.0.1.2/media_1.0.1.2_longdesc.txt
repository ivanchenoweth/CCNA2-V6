La figura muestra dos LAN. Cada LAN tiene un router. Las LAN están conectadas entre sí por un enlace WAN.  La figura muestra el resultado del comando show route i.p en uno de los routers.  El resultado de este comando enumera todas las redes conocidas para el router y las rutas para llegar a esas redes.  Sin embargo, en este router, no se configuró el routing y la tabla de routing está vacía.